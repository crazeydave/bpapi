using System.Net.Http;
using Microsoft.AspNetCore.Builder;
using Microsoft.AspNetCore.Hosting;
using Microsoft.Extensions.Configuration;
using Microsoft.Extensions.DependencyInjection;
using Microsoft.Extensions.Hosting;

namespace Metalmynds.BusinessPortalApi.Web
{
    public class Startup
    {
        static HttpClient http;

        public Startup(IConfiguration configuration)
        {
            Configuration = configuration;
        }

        public IConfiguration Configuration { get; }

        // This method gets called by the runtime. Use this method to add services to the container.
        public void ConfigureServices(IServiceCollection services)
        {

            // Its the Business Portal Client Config 
            services.AddSingleton(new Client.BusinessPortalConfiguration()
            {
                BaseUrl = Configuration["BusinessPortal:Url"],
                Organisation = Configuration["BusinessPortal:Organisation"],
                Domain = Configuration["BusinessPortal:Domain"],
                User = Configuration["BusinessPortal:Username"],
                Password = Configuration["BusinessPortal:Password"]
            });

            services.AddHttpClient<Client.BusinessPortalClient, Client.BusinessPortalClient>();            

            services.AddControllers();

            services.AddSwaggerGen();
        }

        // This method gets called by the runtime. Use this method to configure the HTTP request pipeline.
        public void Configure(IApplicationBuilder app, IWebHostEnvironment env)
        {
            if (env.IsDevelopment())
            {
                app.UseDeveloperExceptionPage();
            }

            app.UseSwagger();

            // Enable middleware to serve swagger-ui (HTML, JS, CSS, etc.),
            // specifying the Swagger JSON endpoint.
            app.UseSwaggerUI(c =>
            {
                c.SwaggerEndpoint("/swagger/v1/swagger.json", "Bpapi");
            });

            app.UseHttpsRedirection();

            app.UseRouting();

            app.UseAuthorization();

            app.UseEndpoints(endpoints =>
            {
                endpoints.MapControllers();
            });
        }
    }
}
